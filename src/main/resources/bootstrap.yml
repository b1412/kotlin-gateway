server:
    port: 9999
spring:
    application:
        name: gateway
    cloud:
        config:
            name: gateway
            password: configpassword
            uri: http://config
            username: leonzhou
